import React from 'react'
import { CollectionType, CommentType, PersonType, UserType } from '../../../../lib/types'

interface iPeoples {
  persons: PersonType[]
}

const Peoples: React.FC<iPeoples> = ({ persons }) => {
  return <></>
}

export default Peoples
